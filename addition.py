# You are given Two Numbers, A and B. If C = A + B. Find C. Note: Round off the output to a single decimal place.
A = float(input())
B = float(input())

C = A + B
print(round(C, 1)) # Rounded off to single decimal as the value indicates.